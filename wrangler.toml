name = "wedding-management"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

# Public environment variables (non-sensitive)
[vars]
# NEXT_PUBLIC_SUPABASE_URL akan di-set via Cloudflare Dashboard
# NEXT_PUBLIC_SUPABASE_ANON_KEY akan di-set via Cloudflare Dashboard

# Jangan taruh secret keys di sini!
# Secret keys (SUPABASE_SERVICE_ROLE_KEY, XENDIT_API_KEY, dll)
# harus di-set via Cloudflare Dashboard atau wrangler secret